# 我的世界

本文档用于统一说明以下六个页面在系统中的定位与协作关系：
- 坐照
- 缘起·性空
- 登堂
- 混沌
- 见微
- 窥径

文档目标：
1. 解释每个页面“为什么存在”
2. 解释每个页面“看什么、做什么、不做什么”
3. 解释每个页面使用了哪些数据
4. 解释这些页面共同构成的人工经验提取与解释闭环

---

## 一、整体定位：六页不是六套系统，而是一条认知链路

这六个页面共同服务于一个核心目标：
**把人工调度经验，从“只在人脑里”变成“可见、可比、可解释、可复盘”的结构化认知。**

它们并不直接改变调度核心求解逻辑（solver/evaluator/objective），而是形成一套“旁路认知系统”：
- 不接管求解
- 不替代调度
- 不改变主调度口径
- 只做经验抽取、经验展示、经验解释与复盘支持

可理解为四层：
1. **观察层**：坐照
2. **规则层**：缘起·性空
3. **解释层**：登堂
4. **复盘/推演认知层**：见微 + 混沌 + 窥径

---

## 二、页面逐一说明

## 1）坐照（rengong）

### 存在意义
坐照是人工经验模块的主入口，强调“如实观察”，不直接做自动决策。
它提供人工调度数据的日常观测与基础判断界面，是经验认知的起点。

### 功能要点
- 作为经验模块入口页
- 承载经验相关入口（如缘起·性空）
- 展示人工调度相关基础信息和状态
- 强调只读与观察，不介入调度主链路

### 主要数据（按现有模块口径）
- 人工调度结果主数据（如 human_dispatch_routes 相关结果）
- 经验分析过程中沉淀的只读统计结果

### 作用与价值
- 让业务与调度人员快速形成“当天/近期人工调度状态”的整体印象
- 作为经验体系总入口，降低学习和操作门槛
- 把“经验”从抽象判断变成可看见的对象

### 能做什么
- 进入经验分析链路
- 查看人工调度经验相关展示
- 进入规则页、解释页等深层页面

### 不做什么
- 不改 solver/evaluator/objective
- 不直接出自动调度方案

---

## 2）缘起·性空（rengong-boundary）

### 存在意义
缘起·性空是**经验规则显化页**：
把人工调度历史中稳定存在的边界、窗口和约束提取出来，让“经验规则”可展示、可核查。

### 功能要点
- 展示店铺收货时间规则
- 展示线路装载规则与装载画像
- 展示里程与装载联合明细
- 展示空间约束规则（例如特定城市线路禁止混线）
- 展示人工经验模型成果卡片（负载边界模型、参考来源权重模型、登堂解释引擎）
- 支持筛选、排序、查询、右键线路明细（已接的只读能力）

### 主要数据来源
- `rengong_store_arrival_windows`
  - 店级到店时间窗口（最早/平均/最晚、早到晚到容忍分钟）
- `rengong_route_load_profiles`
  - 线路装载画像（W1 等口径）
- `rengong_load_rules`
  - 经验模块装载规则定义
- `rengong_route_distance_load_profiles`
  - 线路单程里程与装载统一落库（W1/W2/OTHER）
- `rengong_route_spatial_rules`
  - 空间约束规则（例如承德与张家口禁止混线）

### 作用与价值
- 把“经验边界”从口头规则转成可审计事实
- 让异常更容易定位（超上限、脏数据候选、混线风险）
- 为解释引擎（登堂）提供规则地基

### 能做什么
- 查每条线路的里程/装载与类别
- 查每家店的时间窗口稳定性
- 查已定义的规则与约束
- 查看线路明细（门店顺序、分段里程、原始货量、折算货量）

### 不做什么
- 不直接调度
- 不写入调度主结果
- 不改变核心算法

---

## 3）登堂（dengtang）

### 存在意义
登堂是**解释前台**。
它的核心不在“算出新方案”，而在“解释今天线路为什么这样安排”。

### 功能要点
- 日期选择后，加载当日解释视图
- 展示当日摘要（线路数、稳定复用、变化、风险、复核）
- 展示线路级解释卡片：建议 + 原因
- 展示“参考日还原层”：
  - 今日线路快照
  - 参考日线路快照
  - 门店顺序/货量变化
  - 路线结构变化摘要

### 主要数据来源
- `/rengong/dengtang-explain`（只读接口）
- 接口底层依赖：
  - `human_dispatch_routes`
  - `chaos_replay_tasks`（day_type）
  - `rengong_route_slack_profiles`（负载边界）
  - `rengong_reference_weight_profiles`（参考来源权重）

### 作用与价值
- 让“经验判断”可被复述、可被审查
- 让业务人员知道“为什么保持/为什么调整”
- 为后续人工评审与规则校准提供解释文本基础

### 能做什么
- 看每条线路的建议与原因
- 对比今天与参考日差异
- 快速识别需要复核的线路

### 不做什么
- 不输出自动调度方案
- 不接入求解器
- 不改数据库业务结果

---

## 4）混沌（chaotic simulation 区域）

### 存在意义
混沌页强调“初始条件微变导致结果差异”的调度现实。
它是推演与对比的认知场，不是经验规则落库场。

### 功能要点
- 展示推演说明与对比语境
- 支持批次/策略视角下的结果观察
- 作为“变化认知”入口，帮助理解调度不确定性

### 主要数据
- 与推演展示相关的运行数据与结果摘要（既有链路）
- 不强绑定经验落库表，避免口径混用

### 作用与价值
- 让用户接受“不是唯一解，而是条件驱动解”
- 为后续“为什么变了”提供认知背景

### 能做什么
- 看差异、看趋势、看推演结果
- 与见微形成“输入—结果”对照

### 不做什么
- 不替代经验模块
- 不把经验规则直接写进调度主流程

---

## 5）见微（chaos-jianwei）

### 存在意义
见微是**混沌模块下的输入复盘页**。
它不看“求解结果是否好”，先看“每天给系统的题目是什么”。

### 功能要点
- 按日期查看 replay task 摘要
- 查看当日店铺明细（店铺级货量、来源线路/车辆）
- 查看当日车辆明细（车、司机、原线路集合）
- 支持搜索、排序、固定高度滚动展示

### 主要数据来源
- `chaos_replay_tasks`
- `chaos_replay_task_stores`
- `chaos_replay_task_vehicles`
- 接口：`/chaos/replay-task-view?date=...`（只读）

### 作用与价值
- 把“输入题面”稳定还原出来
- 让业务先确认“题目是否正确”，再谈结果
- 与混沌页配合，形成“输入→演化”完整观察链路

### 能做什么
- 查某天到底有多少店、多少货、多少车
- 定位异常输入日
- 验证日类型（平日/周日）下输入结构差异

### 不做什么
- 不跑求解
- 不评价优劣
- 不生成调度方案

---

## 6）窥径（kuijing）

### 存在意义
窥径是数据分析中枢入口页。
它把系统连接到外部分析能力（当前是 Metabase），用于更高密度的数据洞察。

### 功能要点
- 极简入口
- 一键打开本地分析平台（例如 `http://localhost:3000`）
- 可回到调度系统首页

### 主要数据
- 由 Metabase 连接系统数据库后读取
- 常用表包括经验表、复盘表、人工调度结果表

### 作用与价值
- 承接“结构化分析需求”
- 让非开发人员也能用图表快速验证假设
- 作为长期经营分析的仪表盘入口

### 能做什么
- 打开并使用 Metabase 看板/问题
- 做趋势、分布、异常对比

### 不做什么
- 不在本页执行业务逻辑
- 不修改调度核心

---

## 三、关键数据资产与意义（跨页面）

## A. 人工调度原始事实层
- `human_dispatch_routes`
  - 人工线路结构事实：日期、route_id、store_id、stop_sequence、货量字段、车辆等
  - 是“经验提取”的第一手事实来源

## B. 复盘输入层（见微）
- `chaos_replay_tasks`
- `chaos_replay_task_stores`
- `chaos_replay_task_vehicles`
  - 把历史人工结果反向整理为“每日待调度任务输入”

## C. 经验规则层（缘起·性空）
- `rengong_store_arrival_windows`
  - 店级到店时间窗口经验
- `rengong_route_distance_load_profiles`
  - 线路单程里程+装载统一画像
- `rengong_load_rules`
  - 显式规则（普通线/机场线/脏数据等）
- `rengong_route_spatial_rules`
  - 空间约束规则（禁止混线等）

## D. 解释模型层（登堂）
- `rengong_route_slack_profiles`
  - 每条线路在不同 day_type 下的负载分位画像（p10...p90）
  - 定义稳定区/观察区/风险区
- `rengong_reference_weight_profiles`
  - 基于负载位置（load/p90）的参考来源权重
  - 用于解释“更可能参考前一同类日，还是转向其他参考”

---

## 四、六页联合后，系统“能干什么”

1. **看事实**（见微）
   - 知道每天真实输入是什么
2. **看规则**（缘起·性空）
   - 知道人工经验边界是什么
3. **看解释**（登堂）
   - 知道某条线路今天为何保持/变化
4. **看变化机制**（混沌）
   - 知道条件变化如何带来结果变化
5. **看整体态势**（坐照）
   - 知道经验模块当前状态与入口组织
6. **看分析洞察**（窥径）
   - 知道如何用BI工具持续挖掘规律与异常

合在一起，完成从“经验感知”到“经验表达”的闭环：
- 经验可见
- 规则可查
- 解释可复核
- 异常可定位
- 认知可传承

---

## 五、业务与治理意义

## 1）对业务的意义
- 减少“只靠老师傅记忆”的单点风险
- 提升新人理解调度逻辑的速度
- 让复盘从“结果争论”变成“结构对比”

## 2）对系统演进的意义
- 在不触动主求解核心的前提下，先建设解释与审计能力
- 为未来“规则辅助”与“人机协同”提供可靠地基

## 3）对数据治理的意义
- 强调真实数据来源与可追溯口径
- 将经验规则显式化、版本化、可核查化

---

## 六、明确边界（非常重要）

这六页当前定位是：
**经验提取与解释系统**，不是自动调度引擎。

因此严格边界为：
- 不替代 solver/evaluator/objective
- 不直接写主调度结果
- 不在前台页面触发自动排线
- 不以“黑盒评分”替代人工经验语境

它们做的是：
- 把经验讲清楚
- 把变化看明白
- 把规则沉淀下来

---

## 七、一句话总览

- **坐照**：总入口，观其全局。  
- **缘起·性空**：显规则，见边界。  
- **登堂**：做解释，还原“为何如此”。  
- **见微**：看输入，知题面。  
- **混沌**：看演化，知变化。  
- **窥径**：进分析，成洞察。  

六页合一：
**让人工调度经验，从个人记忆，走向系统能力。**
